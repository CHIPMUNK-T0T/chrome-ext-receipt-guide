# ECサイト領収書ナビゲーター MVP仕様

## MVPの目的

ECサイト領収書ナビゲーターの最初のMVPでは、日本の主要ECサイトで領収書を取得する際に、ユーザーが注文履歴ページへ移動し、領収書関連の操作箇所を見つけやすくすることを目的とする。

本MVPでは、領収書の完全自動取得は行わない。  
ユーザーが各ECサイト上で操作する前提で、数クリックで領収書取得まで進めるように補助する。

## MVP対象サイト

初期対象サイトは以下とする。

- Amazon.co.jp
- 楽天市場
- Yahoo!ショッピング

## MVPで実装する機能

### 1. 拡張機能ポップアップ

Chrome拡張アイコンをクリックすると、ポップアップを表示する。

ポップアップには、以下のサイト選択ボタンを配置する。

- Amazon.co.jp
- 楽天市場
- Yahoo!ショッピング

各ボタンをクリックすると、対象ECサイトの注文履歴ページを新しいタブで開く。

### 2. 注文履歴ページへのショートカット

各ECサイトの注文履歴ページ、または購入履歴ページへ遷移する。

想定導線は以下である。

- Amazon.co.jp：注文履歴ページ
- 楽天市場：購入履歴ページ
- Yahoo!ショッピング：注文履歴ページ

ユーザーが未ログインの場合は、各ECサイト側のログイン画面へ遷移する。  
本アプリはログイン処理を行わない。

### 3. ガイドバー表示

対象ECサイト上で、ページ上部に固定表示のガイドバーを表示する。

ガイドバーには、次にユーザーが行うべき操作を簡潔に表示する。

表示例：

```text
Amazonの注文履歴ページを開きました。
対象の注文を探し、「領収書等」から領収書または購入明細書を表示してください。
````

```text
楽天市場の購入履歴ページを開きました。
対象注文の「注文詳細」を開き、「領収書・請求書」から発行してください。
```

```text
Yahoo!ショッピングの注文履歴ページを開きました。
対象注文の詳細画面を開き、領収書または明細に関する表示を確認してください。
```

### 4. 領収書関連要素のハイライト

対象ページ内のリンクやボタンを走査し、領収書取得に関連しそうな要素をハイライトする。

対象とする文言の例：

* 領収書
* 領収書等
* 領収書・請求書
* 購入明細書
* 適格請求書
* 注文詳細
* 明細
* 注文履歴

ハイライトは、ユーザーが押すべき候補を見つけやすくするための補助であり、自動クリックは行わない。

### 5. ダウンロードPDFの保存名整理

領収書PDFや明細PDFをダウンロードする際に、保存ファイル名を整理する。

初期の保存先テンプレートは以下とする。

```text
Receipts/{SiteName}/{yyyy}-{MM}/{site}_{yyyy}-{MM}-{dd}_{originalFilename}
```

例：

```text
Receipts/Amazon/2026-05/amazon_2026-05-10_receipt.pdf
Receipts/Rakuten/2026-05/rakuten_2026-05-10_receipt.pdf
Receipts/Yahoo/2026-05/yahoo_2026-05-10_receipt.pdf
```

保存先はChromeのダウンロードフォルダ配下の相対パスとする。

### 6. 設定保存

以下の設定をブラウザ内に保存する。

* ガイド表示の有効・無効
* ハイライト表示の有効・無効
* ダウンロードファイル名整理の有効・無効
* サイト別の有効・無効

設定は `chrome.storage.local` に保存し、外部サーバーへ送信しない。

## MVPで実装しない機能

以下はMVPでは実装しない。

* 自動ログイン
* ログイン情報の保存
* 注文履歴の自動取得
* 注文履歴の一覧化
* 領収書の一括自動ダウンロード
* 購入金額や注文番号の自動収集
* CAPTCHA回避
* 外部サーバーへのデータ送信
* 会計ソフト連携
* クラウド同期

## 権限方針

Chrome拡張の権限は、必要最小限にする。

想定する権限：

* `tabs`
* `scripting`
* `storage`
* `downloads`

対象サイトへのアクセス権限は、日本の対象ECサイトに限定する。

## 動作確認方針

### 単体テスト

以下を対象にテストする。

* URLから対象サイトを判定できること
* ファイル名テンプレートが正しく生成されること
* 危険なファイル名文字を除去できること
* キーワード判定が正しく動作すること

### ローカルHTMLテスト

Amazon、楽天、Yahoo!ショッピングの疑似HTMLを作成し、ガイド表示とハイライト表示が動作することを確認する。

### 実サイトでの手動確認

実際のECサイト上で以下を確認する。

* 注文履歴ページが開くこと
* ガイドバーが表示されること
* 領収書関連の要素がハイライトされること
* PDFダウンロード時に保存名が整理されること
* 未ログイン時にログイン画面へ遷移しても問題ないこと

## MVP完了条件

以下を満たしたらMVP完了とする。

* Chrome拡張としてローカル読み込みできる
* ポップアップから3サイトの注文履歴ページを開ける
* 対象サイト上でガイドバーを表示できる
* 領収書関連のリンク・ボタンをハイライトできる
* PDFダウンロード時にファイル名を整理できる
* 設定がブラウザ内に保存される
* 外部サーバーにデータを送信しない
